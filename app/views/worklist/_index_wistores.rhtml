<h3>stores</h3>

<div class="worklist_category_description">
the engine dispatches workitems to participants, the regular expressions are used to match those participant names to store names.<br>
there is an implicit 'users' workitem stores, where unmapped workitems are put (if there is a user with the same name as the participant name of the workitem)
</div>

<%= 
    render_record_table(
        WiStore,
        records,
        [ [ :name, "40%" ], [ :regex, "44%" ] ])
-%>
